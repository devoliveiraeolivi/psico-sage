# Domínio base (usado para traefik.DOMAIN)
DOMAIN=seudominio.com.br

# Email para certificados Let's Encrypt
ACME_EMAIL=seu@email.com

# Senha do dashboard Traefik (gerar com: htpasswd -nB admin)
# Formato: usuario:hash — escape $ duplicando: $ -> $$
TRAEFIK_DASHBOARD_AUTH=admin:$$2y$$05$$...
